Ունենք n չափանի զանգված տարր պարունակող զանգվածի,որի  տարրերն են համարվում  1, n+1 չկրկնվող թվերն են: 
•	Պահանջվում է գտնել հաջորդականության պակասող թիվը:
#include <iostream>
using namespace std;
//int getnum(int a[], int n)
//{
//    int b = 0;
//    int sum1 = ((1 + n+1) * (n+1)) / 2;
//    int sum2 = 0;
//    for (int i = 0; i < n; i++)
//    {
//        sum2 += a[i];
//    }
//    b = sum1 - sum2;
//    return b;
//}


int main()
{
   /* while (true)
    {
        int a, b;
        cout << endl << "Write a and b" << endl;
        cin >> a;
        cin >> b;
        if (a < 0) a = -a;
        if (b < 0) b = -b;
        int baj = getbaj(a, b);
        cout << endl << baj << endl;
    }*/


   /* int n;
    cout << "Write n" << endl;
    cin >> n;
    int a[50];
    for (int i = 0; i < n; i++)
    {
        cin >> a[i];
    }
    int b = getnum(a, n);
    cout << endl << b << endl;*/
